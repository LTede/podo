# 버전 · 배포 관리

## 브랜치 전략

| 브랜치 | 용도 | 배포 |
|---|---|---|
| `main` | **프로덕션** — 보여주기/오픈 가능한 안정판만 | Vercel Production (고정 URL) |
| `claude/podowoman-ui-ux-redesign-wjbc1v` | **작업/테스트** — 개발이 계속 진행되는 브랜치 | Vercel Preview (테스트 URL) |
| `design/<n>-<이름>` | 대안 시안 실험 (필요 시) | Vercel Preview |

- 개발은 항상 작업 브랜치에서 진행하고, 확인이 끝난 상태만 `main`으로 머지한다.
- `main` 머지 = 프로덕션 배포. 머지 시점마다 버전 태그를 남긴다.

## Vercel 연결 (1회 설정)

1. Vercel 프로젝트 → **Settings → Git → Production Branch**를 `main`으로 지정
2. 이후 자동으로:
   - `main` 푸시 → **프로덕션 URL** (`podo-〇〇〇.vercel.app`) 갱신
   - 작업 브랜치 푸시 → **프리뷰 URL** 자동 발급 (브랜치 고정 프리뷰 링크 + 커밋별 링크)
3. 팀장 보고는 프로덕션 URL로, 내부 확인은 프리뷰 URL로 구분해 사용

## 버전 태그 규칙

- 형식: `v<major>.<minor>.<patch>-design<N>` — 시안 계열(N)과 완성도를 함께 표기
- 시안이 갈리면 N이 증가, 같은 시안의 개선은 minor/patch 증가
- 태그는 `main` 머지 커밋에 부여

## 버전 히스토리

| 버전 | 날짜 | 내용 |
|---|---|---|
| `v1.0.0-design1` | 2026-08-11 | **시안 1 — 포도줄기(노아르)**: PODO NOIR 다크 럭셔리, 회전 인트로, 아이덴티티 랜딩, 포도줄기 척추(꽃관·꽃잎·빛웅덩이), 7챕터 원형 나선, 8센터 콘텐츠 전수 반영, 전환 레이어(상담 폼/플로팅 바/증상 네비/FAQ 28문항/신뢰 스탯), SEO(JSON-LD·sitemap), 감사 1회전 통과 |
| `v1.0.0-design2` | 2026-08-11 | **시안 2 — 포도 아이보리(라이트 럭셔리)**: 시안 1과 구조·콘텐츠·연출 동일, 팔레트 반전 — 아이보리·샴페인 바탕 + 딥 와인 포인트, 흰 유리 패널, 수채 톤 WebGL(가산합성 제거), 인트로는 딥와인 유지("보석함이 열리는" 전환). 음지 업계 오독 리스크 해소 목적. 브랜치 `design/2-ivory` |

## 릴리스 절차 (반복)

1. 작업 브랜치에서 개발 → 푸시 → 프리뷰 URL로 확인
2. 확정되면 `main`으로 머지 → 프로덕션 자동 배포
3. `git tag -a v<버전> -m "<내용>"` → 태그 푸시 → 본 문서 히스토리 갱신
